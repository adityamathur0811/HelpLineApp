import 'package:flutter/material.dart';
class Info extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("this is help",style: TextStyle(fontSize: 20.0,color: Colors.white),)),
    );
  }
}
